/** Shared runtime constants — single source of truth for main process and tray. */

export const NODE_VERSION = 'v22.14.0';
export const GATEWAY_PORT = 18789;
export const OPENCLAW_DIR_NAME = '.openclaw';
